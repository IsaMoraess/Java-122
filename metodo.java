public class metodo {
    public static void main(String[ ] args){
        Sobrecarga sob = new Sobrecarga();//instancia um objeto da classe Sobrecarga
        sob.calcula(2.5); //chama o método calcula( ) e passa 1 parâmetro
        sob.calcula(7,3); //chama o método calcula( ) e passa 2 parâmetros
    }
}
